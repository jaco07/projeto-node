<div class="book-page">

    <div>
        <h1>{{book.title}}</h1>
        <p>Esse livro tem {{book.nm_paginas}} Páginas</p>
        <div class="action">
            <a href="/books/edit/{{book.id}}">Editar</a>
            <a href="/books/deleteBooks/{{book.id}}">Excluir</a>
        </div>
    </div>

    <a href="/books">Voltar</a>
</div>

<style>
     a{
        text-decoration: none;
        color: black;
        font-family: 'Sedgwick Ave Display', cursive;
    }
    a:hover{
        color:aqua;
    }
    .book-page{
        align-items: center;
        text-align: center;
        display: flex;
        flex-direction: column;
        gap: 20px;

        justify-content: center;
    }
</style>